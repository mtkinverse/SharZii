# Sharezii
A web-based system that allows exchanging messages via _Web-sockets_ and calls via _WebRTC protocol_

# Running the development server
The development server can be experienced after just two commands
1 - Install the dependencies : _npm install_
2 - Run the server : _npm run dev_